# --- Claude Code instructions (NOT part of the public project) ---
# These hold the build-pack/spec that drives development; never commit them.
CLAUDE.md
private_docs/
PROGRESS.md
# Local Claude Code tooling (installed skills, local settings) — not the product.
.claude/

# --- Generated Xcode project (source of truth is project.yml + xcodegen) ---
# Run `xcodegen generate` to (re)create it; never commit the generated project.
platform/macos/84Key.xcodeproj/
# Per-machine signing override (keeps a personal Team ID out of git, if used).
platform/macos/*.local.xcconfig

# --- macOS ---
.DS_Store

# --- Xcode ---
build/
DerivedData/
*.xcuserstate
*.xcuserdatad/
xcuserdata/
*.moved-aside
*.hmap
*.ipa

# --- Swift Package Manager ---
.build/
.swiftpm/
Package.resolved

# --- Distribution artifacts ---
*.dmg
*.zip
*.app

# --- Local-only test fixtures (e.g. pasted articles that may be copyrighted) ---
core/tests/cases/test_1.txt

# --- Misc ---
*.log
.env

# AI debate transcripts (local only)
debates/
